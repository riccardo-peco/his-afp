export interface TriageColor {
  code: string;
  displayName: string;
  priority: number;
  hexValue: string;
}
export interface Pathology {
  code: string;
  description: string;
}
export interface ArrivalMode {
  code: string;
  description: string;
}
